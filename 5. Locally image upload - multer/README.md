# Locally Upload Image using Multer - Node JS
This is a simple project about uploading image locally using multer. It covers file validation also (the file is image or not).

## Install Dependencies
    npm install

## Run
```
npm start
```